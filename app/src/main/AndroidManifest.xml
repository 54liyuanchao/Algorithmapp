<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a54liyuanchao.yuanchaoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BFSActivity"
            android:label="@string/title_activity_bfs" />
        <activity
            android:name=".DFSActivity"
            android:label="@string/title_activity_dfs" />
        <activity
            android:name=".BSActivity"
            android:label="@string/title_activity_bs" />
        <activity
            android:name=".MSActivity"
            android:label="@string/title_activity_ms" />
        <activity
            android:name=".QSActivity"
            android:label="@string/title_activity_qs" />
        <activity
            android:name=".TIActivity"
            android:label="@string/title_activity_ti"></activity>
    </application>

</manifest>